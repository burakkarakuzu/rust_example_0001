fn maximum(list:&[i32]) -> i32{
	let first=list[0];
	for &item in list{
	 if item >= first{
	 	let _first =item;
} 
}
first 
}
fn main(){
 let v=vec![1,2,3,4,5];
 let m=maximum(&v);
 println!("En buyuk sayi:{}",m);
}